// Mock receipt data has been removed. All bill data is dynamically parsed from the uploaded receipt image.
export const SAMPLE_RECEIPT_IMAGE = "";
export const DEFAULT_PARTICIPANTS: any[] = [];
export const DEFAULT_ASSIGNMENTS: Record<string, string[]> = {};
